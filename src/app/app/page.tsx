export default function AppShell() {
  return (
    <main className="mx-auto flex min-h-dvh max-w-5xl flex-col gap-4 px-6 py-10">
      <h2 className="text-xl font-semibold tracking-tight">App shell</h2>
      <p className="text-muted-foreground">
        Placeholder route at <code>/app</code> (App Router).
      </p>
    </main>
  );
}

